<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xbk.knowledge.infrastructure.mapper.TaskTypeMapper">

    <!-- 任务类型结果映射 -->
    <resultMap id="TaskTypeMap" type="com.xbk.knowledge.domain.model.entity.TaskType">
        <id column="id" property="id"/>
        <result column="org_id" property="orgId"/>
        <result column="task_name" property="taskName"/>
        <result column="task_code" property="taskCode"/>
        <result column="description" property="description"/>
        <result column="preferred_model_id" property="preferredModelId"/>
        <result column="fallback_model_ids" property="fallbackModelIds"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- 写入任务类型配置 -->
    <insert id="insertTaskType" parameterType="com.xbk.knowledge.domain.model.entity.TaskType" useGeneratedKeys="true"
            keyProperty="id">
        insert into ai_task_type (
            org_id,
            task_name,
            task_code,
            description,
            preferred_model_id,
            fallback_model_ids,
            created_at,
            updated_at
        ) values (
            #{orgId},
            #{taskName},
            #{taskCode},
            #{description},
            #{preferredModelId},
            #{fallbackModelIds},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <!-- 更新任务类型配置 -->
    <update id="updateTaskType" parameterType="com.xbk.knowledge.domain.model.entity.TaskType">
        update ai_task_type
        set task_name = #{taskName},
            task_code = #{taskCode},
            description = #{description},
            preferred_model_id = #{preferredModelId},
            fallback_model_ids = #{fallbackModelIds},
            updated_at = #{updatedAt}
        where id = #{id}
    </update>

    <!-- 删除任务类型配置 -->
    <delete id="deleteTaskTypeById">
        delete from ai_task_type
        where id = #{id}
    </delete>

    <!-- 按 ID 查询任务类型 -->
    <select id="findById" resultMap="TaskTypeMap">
        select
            id,
            org_id,
            task_name,
            task_code,
            description,
            preferred_model_id,
            fallback_model_ids,
            created_at,
            updated_at
        from ai_task_type
        where id = #{id}
    </select>

    <!-- 按任务编码查询任务类型 -->
    <select id="findByTaskCode" resultMap="TaskTypeMap">
        select
            id,
            org_id,
            task_name,
            task_code,
            description,
            preferred_model_id,
            fallback_model_ids,
            created_at,
            updated_at
        from ai_task_type
        where task_code = #{taskCode}
    </select>

    <!-- 分页查询任务类型 -->
    <select id="findPage" resultMap="TaskTypeMap">
        select
            id,
            org_id,
            task_name,
            task_code,
            description,
            preferred_model_id,
            fallback_model_ids,
            created_at,
            updated_at
        from ai_task_type
        order by updated_at desc, id asc
        limit #{pageSize} offset #{offset}
    </select>

    <!-- 统计任务类型总数 -->
    <select id="countAll" resultType="long">
        select count(1)
        from ai_task_type
    </select>

</mapper>
