<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xbk.knowledge.infrastructure.mapper.ApiKeyMapper">

    <resultMap id="SysApiKeyMap" type="com.xbk.knowledge.domain.model.entity.SysApiKey">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="access_key" property="accessKey"/>
        <result column="secret_hash" property="secretHash"/>
        <result column="scopes" property="scopes"/>
        <result column="status" property="status"/>
        <result column="expire_at" property="expireAt"/>
        <result column="last_used_at" property="lastUsedAt"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <sql id="SysApiKeyColumns">
        id,
        tenant_id,
        owner_user_id,
        access_key,
        secret_hash,
        scopes,
        status,
        expire_at,
        last_used_at,
        created_at,
        updated_at
    </sql>

    <select id="findPage" resultMap="SysApiKeyMap">
        select
        <include refid="SysApiKeyColumns"/>
        from sys_api_key
        where tenant_id = #{tenantId}
        <if test="ownerUserId != null">
            and owner_user_id = #{ownerUserId}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
        order by updated_at desc, id desc
        limit #{pageSize} offset #{offset}
    </select>

    <select id="count" resultType="long">
        select count(1)
        from sys_api_key
        where tenant_id = #{tenantId}
        <if test="ownerUserId != null">
            and owner_user_id = #{ownerUserId}
        </if>
        <if test="status != null">
            and status = #{status}
        </if>
    </select>

    <insert id="insertApiKey" parameterType="com.xbk.knowledge.domain.model.entity.SysApiKey" useGeneratedKeys="true"
            keyProperty="id">
        insert into sys_api_key (
            tenant_id,
            owner_user_id,
            access_key,
            secret_hash,
            scopes,
            status,
            expire_at,
            last_used_at,
            created_at,
            updated_at
        ) values (
            #{tenantId},
            #{ownerUserId},
            #{accessKey},
            #{secretHash},
            #{scopes},
            #{status},
            #{expireAt},
            #{lastUsedAt},
            #{createdAt},
            #{updatedAt}
        )
    </insert>

    <select id="findById" resultMap="SysApiKeyMap">
        select
        <include refid="SysApiKeyColumns"/>
        from sys_api_key
        where id = #{id}
        limit 1
    </select>

    <update id="updateStatus">
        update sys_api_key
        set status = #{status},
            updated_at = now()
        where id = #{id}
          and tenant_id = #{tenantId}
    </update>

</mapper>
